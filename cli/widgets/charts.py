"""Plotly candlestick chart with zoom / pan / fullscreen, plus a highlighted forecast band
drawn immediately to the right of the last real candle. Redraws on index/horizon/forecast change."""
def candle_chart(series, forecast: dict | None = None):
    # import plotly.graph_objects as go ; build candlestick + shaded forecast region
    raise NotImplementedError

"""Prediction screen: pick index + horizon -> forecast range, confidence, Long/Short/Stay-out,
profit estimator (amount in ILS/USD/EUR), and a dynamic chart with the forecast band."""
def render():
    # res = api.predict(index, horizon, amount, currency)
    # show range/confidence/recommendation; draw chart via widgets.charts
    raise NotImplementedError
